- MPI didn't work on bw-uni-cluster. then I decided to run a separate ground truth simulation as its configuration doesn't change so much.

- functions should get cleaned and organized.
	- add other fitting functions to dask
	- separate and rap functions for dask

- check formula for tau

- notebooks should be organized for comprehension

